@model MusicEShop.Domain.DomainModels.CartItem

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-lg-4 col-md-6 col-12 mt-4">
            <form asp-action="AddToCartConfirmed" class="shadow p-4 rounded bg-light">
                <input type="hidden" asp-for="AlbumId" />

                <div class="form-group mb-3">
                    <label asp-for="Quantity" class="form-label fw-bold">Quantity</label>
                    <input asp-for="Quantity" defaultValue="1" type="number" min="1" class="form-control" placeholder="Enter quantity" />
                    <span asp-validation-for="Quantity" class="text-danger"></span>
                </div>

                <div class="d-flex justify-content-between">
                    <a asp-action="Index" class="btn btn-outline-secondary">Back to List</a>
                    <button type="submit" class="btn btn-danger">Add To Cart</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
